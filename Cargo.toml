[package]
name = "khmercut"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4.6", features = ["derive"] }
crfsuite = "0.3.1"
regex = "1.10.0"

[profile.release]
opt-level = 3
